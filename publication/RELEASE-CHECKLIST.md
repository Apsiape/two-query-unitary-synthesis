# Release readiness — v1.0.4, 2026-09-13

**Private GitHub release candidate, not a public publication.** At the author's
request on 2026-09-13, the repository was created as **private** at
`https://github.com/Apsiape/two-query-unitary-synthesis`. Commit and push are
authorized; making it public, creating a public release, and Zenodo publication
remain on hold. No Zenodo DOI has been created by this review.

## Mathematical gate

- [x] Fresh-context, refutation-first AI audit of the working manuscript and its
  load-bearing dependencies. See `audit/2026-09-13-independent-audit.md`.
- [x] Fixed-architecture quantifier and rate-function typing repaired in the
  transfer theorem and companion note.
- [x] Exact derangement recurrence replaces the false finite approximation;
  displayed calibration values are unchanged.
- [x] Numerical local optima, clean-output compression, and register dimensions
  are identified accurately. Main theorem statements retain their scope.
- [x] Limited AI and Lean verification disclosed. No external peer review,
  complete formalization, exhaustive novelty search, or full AK resolution claimed.
- [x] Expanded the disclosure to the I3322 author-responsibility and transparency
  standard, adapted to Q2's actual finite-check and Lean scope. No analytic claims
  changed in this editorial update.

## Reproducibility and artifacts

- [x] Nine finite verifiers passed on Python 3.13 / NumPy 2.4.3.
- [x] Lean 4.30.0 / pinned Mathlib: `lake build` passed (2084 jobs).
- [x] Fresh `lake env lean axiom_check.lean`: seven declarations; only
  `propext`, `Classical.choice`, `Quot.sound`, no `sorryAx`.
- [x] Self-contained TeX and Markdown source consistency check passed.
- [x] Citation metadata validated against the official CFF 1.2.0 schema.
- [x] Three-pass isolated PDF build passed, without missing references or overfull boxes.
- [x] All 17 manuscript pages visually inspected; final build/render must remain paired.
- [x] Paper/prose CC BY 4.0 and code MIT licensing explicitly separated.
- [x] Source ZIP integrity and per-file SHA-256 manifest checked (43 source files).
- [x] `git diff --check` passed; the existing uncommitted work was preserved.
- [x] A scoped scan of the public working files found no GitHub-token/private-key
  signature matches. This is not an exhaustive secret-detection guarantee.

Local diagnostic and build jobs were memory/time capped and completed; no test
worker was left running. These finite checks supplement the analytic audit, not
replace it. Source archives and checksums are generated by `tools/package_release.py`.

## Public publication sequence still to execute

1. Obtain the author's explicit **public** publication decision and final reading approval. Confirm
   version/date and author details; no affiliation or ORCID has been guessed.
2. Commit the reviewed sources without discarding pre-existing edits. Inspect the
   commit/archive being published. The local source ZIP records its base commit
   and dirty-working-tree status; it is not represented as a clean Git snapshot.
3. Only after that authorization, change the existing private repository's
   visibility to public, tag the reviewed commit `v1.0.4`, and attach the PDF,
   source ZIP and checksums to a release. Until then, keep the repository private.
4. With an authenticated Zenodo session/token, create a preprint deposit using
   `publication/zenodo-metadata.json`. Upload **only** `paper/paper.pdf` and
   `release/arxiv-source.zip` to the CC BY 4.0 manuscript record. Keep the MIT
   verification software in the linked GitHub repository; do not inadvertently
   apply the manuscript-only license to the entire code archive.
5. Publish the record, verify its public files and DOI, then add the actual DOI
   to the preferred citation, README and release record. Do not invent a DOI.
6. Record immutable commit/tag, DOI, artifact hashes and public URLs. If the
   manuscript itself is changed to include a reserved DOI before publication,
   rebuild, check the affected pages and regenerate checksums first.

There is no arXiv submission in this workflow. A Zenodo stamp establishes a
public archival record, not mathematical peer-review acceptance.
